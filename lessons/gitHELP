git config --global user.name "FIRST_NAME LAST_NAME"
git config --global user.email "MY_NAME@example.com"

пишется 1 раз:
git init: связывает гит с вашим проектом
git remote add origin ссылка на ваш репозиторий: связывает ваш проект к репозиторию

всегда:
git add. : добавляет файлы
git commit -m 'название'
git push origin main


git rm --cached -r namefile : стирает файл



git clone url репозитория: скачать чей-то репозиторий

git remote remove origin

cd

git branch: показывает ветки
git branch name: создает ветку
bit checkout name: переключает ветки
git merge name: слияет ветку которую вы написали в ветку в которой вы находитесь


git pull
как удалить ветку